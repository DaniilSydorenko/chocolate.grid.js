"use strict";

const errors = require('../app/errors/Errors');

describe("Tests for Errors", () => {
    let message = null;

    beforeEach(() => {
        message = "Method create grid";

    });

    // it("returns", () => expect(errors.throwError(message, 'E_002')).toThrow("Method create grid: Invalid type of parameter"));
});
